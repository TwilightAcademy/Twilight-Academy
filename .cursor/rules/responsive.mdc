---
description: 黄昏学院网站需同时适配手机与电脑
alwaysApply: true
---

# 手机与电脑

- 一套页面同时服务两种屏幕。宽度低于 640px 时，首页「企划内容」与「如何加入企划」改为上下排列；更宽时保持左右并排。
- 这两个入口在简介下方。再往下是 `images/background/split_line.png`，然后是三格链接。
- 整页背景是 `css/base.css` 里的点阵，手机和电脑使用同一图案，随屏幕重复铺开。
- 横幅比例是 12:4.2。导出 2160×756，主体放在正中约七成宽度。
- 内页图片至少 1600 像素宽，放进对应的 `images/` 文件夹，页面里会缩小适配。
- 可点区域在手机上保持好按。不要把信息只放在鼠标悬停上。
